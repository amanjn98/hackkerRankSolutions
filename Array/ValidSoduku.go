package Array

func isValidSudoku(board [][]byte) bool {
	//rowCount := make([]int, 10)
	//colCount := make([]int, 10)
	for i := 0; i < len(board); i++ {
		for j := 0; j < len(board[i]); j++ {
			if board[i][j] != '.' {

			}
		}
	}

	return false
}
